# Reusable upgrade step: 8.1z3 → 9.1 hotfix (IBMCEPH15746+).
# Bootstrap uses rhcs-version: 8.1 and release: z3 (not released/GA).
# Insert after 8.1z3 cluster bootstrap/OSD deploy, before RGW apply on hotfix.
# Target hotfix image comes from the pipeline manifest (cephci config).
#
#   - test:
#       abort-on-fail: true
#       name: upgrade 8.1z3 to hotfix
#       desc: Upgrade cluster from 8.1z3 to 9.1 hotfix.
#       module: test_cephadm_upgrade.py
#       verify_cluster_health: true
#       config:
#         command: start
#         service: upgrade
#         base_cmd_args:
#           verbose: true
